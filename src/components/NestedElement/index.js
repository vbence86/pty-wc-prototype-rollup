import NestedElement from './NestedElement.svelte';

// wraps the module in order to be able to import this component
// just by referencing the folder name.
// e.g.: import MyComponent from 'src/my-component'
export default NestedElement;
